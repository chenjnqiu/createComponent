---
name: Icon 图标
route: /Icon
menu: 组件
---

import { Playground, Props } from 'docz'
import Icon from './'
import './index.less'


# Icon 图标

## 参数
<Props of={Icon} />

## 基础用法
<Playground>
  <Icon type="search" />
</Playground>

## 旋转图标
<Playground>
  <Icon type="close-circle-o" spin={true} />
</Playground>

## 全部图标
<Playground>
  {() => {
    const typeList = ['subtract-circle-o', 'close-circle-o', 'check-circle-o','search','bell-o',
    'customer-o','down','up','settings-o','plus-circle-o','reverse','forward','closed','lock','delete','menu','position']
    return (
      <div>
         {typeList.map(item =>  <Icon type={item} />)}
      </div>
    )
  }}
</Playground>
